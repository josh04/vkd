#pragma once
        
#include <memory>
#include "vulkan.hpp"

namespace vkd {
    class Exr {
    public:
        Exr() = default;
        ~Exr() = default;
        Exr(Exr&&) = delete;
        Exr(const Exr&) = delete;
    private:

    };
}